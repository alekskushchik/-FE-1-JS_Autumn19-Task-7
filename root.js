const ROOT_HEADER = document.getElementById('header');
const ROOT_UI = document.getElementById('btnContainer');
const ROOT_PRODUCTS = document.querySelector('.products');
const ROOT_SHOPPINGCART = document.getElementById('shopping-cart');
const ROOT_FOOTER = document.getElementById('footer');